[build]
  command = "node --version && npm --version && rm -rf .next node_modules/.cache && npm ci && npm run build:netlify"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20.11.0"

[[plugins]]
  package = "@netlify/plugin-nextjs"
