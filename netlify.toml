[build]
  command = "rm -rf .next node_modules/.cache && npm ci && npm ls next && npm run build:netlify"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
